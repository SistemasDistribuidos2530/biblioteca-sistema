# .env.example - biblioteca-sistema (Sede)
# Copiar a .env y adaptar según máquina (Sede 1 vs Sede 2)

# --- Puertos y enlaces GC ---
GC_REP_BIND=tcp://0.0.0.0:5555
GC_PUB_BIND=tcp://0.0.0.0:5556
GC_ACTOR_PRESTAMO=tcp://localhost:5560

# --- Gestor Administrador (GA) ---
GA_PRIMARY_BIND=tcp://0.0.0.0:6000
GA_SECONDARY_BIND=tcp://0.0.0.0:6001
GA_ROLE=primary   # Cambiar a 'secondary' en Sede 2

# --- Replicación (si aplica) ---
GA_REPL_PUSH_ADDR=tcp://10.43.101.220:7001   # Dirección a la que el primary hace push
GA_REPL_PULL_BIND=tcp://0.0.0.0:7001         # Bind del secondary para recibir

# --- Workers GC Multihilo ---
GC_NUM_WORKERS=10

# --- Logs y monitoreo ---
MONITOR_INTERVAL_S=3
FAILOVER_HEARTBEATS_THRESHOLD=3

# --- Misc ---
PYTHONUNBUFFERED=1

